# =============================================================================
# Kin AI Raspberry Pi Client - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# Usage: cp .env.example .env

# =============================================================================
# REQUIRED VARIABLES (must be set or application will exit)
# =============================================================================

# Device Identification
DEVICE_ID=raspberry-pi-001                           # Unique identifier for this device

# Supabase Authentication
SUPABASE_URL=https://xxxxx.supabase.co              # Your Supabase project URL
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6...  # Supabase anonymous/public key
EMAIL=user@example.com                               # User email for authentication
PASSWORD=your_secure_password                        # User password

# API Keys
ELEVENLABS_API_KEY=sk_xxxxxxxxxxxxx                 # ElevenLabs API key for TTS
PICOVOICE_ACCESS_KEY=xxxxxxxxxxxxx                  # Picovoice access key for wake word

# =============================================================================
# OPTIONAL VARIABLES (have defaults)
# =============================================================================

# Backend Configuration
CONVERSATION_ORCHESTRATOR_URL=ws://localhost:8001/ws # WebSocket URL for orchestrator
                                                     # For production: wss://your-domain.com/ws

# Wake Word Settings
WAKE_WORD=porcupine                                  # Wake word to detect
                                                     # Options: porcupine, jarvis, alexa, 
                                                     # computer, terminator, etc.
                                                     # (depends on Porcupine built-in keywords)

# LED Visual Feedback
LED_ENABLED=true                                     # Enable/disable LED feedback
                                                     # Set to "false" to disable
                                                     # Requires ReSpeaker with pixel_ring

LED_BRIGHTNESS=60                                    # LED brightness (0-100)
                                                     # Default: 60% for subtle, calming effect

# OpenTelemetry (Observability)
OTEL_ENABLED=true                                    # Enable/disable telemetry
                                                     # Set to "false" to disable all telemetry

OTEL_EXPORTER_ENDPOINT=http://localhost:4318        # HTTP endpoint for OTEL collector
                                                     # Default uses local collector

ENV=production                                       # Environment name
                                                     # Options: development, staging, production

# =============================================================================
# NOTES
# =============================================================================
#
# Audio Configuration:
# - SAMPLE_RATE is hardcoded to 16000 Hz (optimized for ElevenLabs and ReSpeaker)
# - Client prioritizes ReSpeaker 4 Mic Array with hardware AEC
# - Automatically falls back to best available mic/speaker if ReSpeaker not found
#
# Telemetry:
# - Includes traces, spans, and structured logging (no metrics)
# - Gracefully degrades if telemetry unavailable or disabled
#
# Signal Handling:
# - SIGUSR1: Terminates current conversation only
# - SIGINT/SIGTERM (Ctrl+C): Full shutdown
#
# =============================================================================
