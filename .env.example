# =============================================================================
# REQUIRED - Device Credentials (from admin portal)
# =============================================================================
DEVICE_ID=<your-device-uuid>
DEVICE_PRIVATE_KEY=<base64-encoded-ed25519-private-key>

# =============================================================================
# OPTIONAL - Speaker Monitor Settings (for ground-truth agent timing)
# =============================================================================
# Enable loopback-based speaker monitoring
# First run: sudo ./raspberry-pi-client-wrapper/speaker-monitor/install-loopback.sh
# SPEAKER_MONITOR_MODE=loopback

# ALSA device name for speaker monitoring (default: speaker_monitor)
# SPEAKER_MONITOR_LOOPBACK_DEVICE=speaker_monitor

# Enable debug logging for speaker monitor
# SPEAKER_MONITOR_DEBUG=1

# =============================================================================
# OPTIONAL - Turn Tracker Settings
# =============================================================================
# VAD probability threshold for speech detection (0.0-1.0, default: 0.5)
# TURN_TRACKER_VAD_THRESHOLD=0.5

# --- User (mic) settings - allow short utterances like "Yep" ---
# Seconds of silence before ending user turn (default: 2.5)
# TURN_TRACKER_USER_SILENCE_TIMEOUT=2.5

# Minimum user turn duration - shorter to capture "Yep" etc (default: 0.15)
# TURN_TRACKER_USER_MIN_TURN_DURATION=0.15

# Minimum continuous speech to start user turn (default: 0.08)
# TURN_TRACKER_USER_MIN_SPEECH_ONSET=0.08

# --- Agent (speaker) settings ---
# Seconds of silence before ending agent turn (default: 2.5)
# TURN_TRACKER_AGENT_SILENCE_TIMEOUT=2.5

# Minimum agent turn duration (default: 0.2)
# TURN_TRACKER_AGENT_MIN_TURN_DURATION=0.2

# Minimum continuous speech to start agent turn (default: 0.08)
# TURN_TRACKER_AGENT_MIN_SPEECH_ONSET=0.08

# Debounce window in seconds (default: 0.5)
# TURN_TRACKER_DEBOUNCE_WINDOW=0.5

# =============================================================================
# OPTIONAL - Telemetry Settings
# =============================================================================
# Enable OpenTelemetry (default: true)
# OTEL_ENABLED=true

# Skip WiFi setup mode on boot (default: true)
# SKIP_WIFI_SETUP=true
