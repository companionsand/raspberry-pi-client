# Raspberry Pi Client Configuration
# Copy this file to .env and fill in your values
# The wrapper's install.sh will automatically populate many of these

# =============================================================================
# DEVICE CONFIGURATION (Required)
# =============================================================================

# Unique device identifier
DEVICE_ID=your-device-id-here

# Deployment environment
ENV=production

# =============================================================================
# SUPABASE AUTHENTICATION (Required)
# =============================================================================

# Supabase project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous key
SUPABASE_ANON_KEY=your-supabase-anon-key-here

# User credentials for authentication
EMAIL=your-email@example.com
PASSWORD=your-password-here

# =============================================================================
# BACKEND SERVICES (Required)
# =============================================================================

# Conversation orchestrator WebSocket URL
CONVERSATION_ORCHESTRATOR_URL=wss://your-orchestrator.onrender.com/ws

# =============================================================================
# API KEYS (Required)
# =============================================================================

# ElevenLabs API key for voice synthesis
ELEVENLABS_API_KEY=your-elevenlabs-api-key-here

# Picovoice access key for wake word detection
PICOVOICE_ACCESS_KEY=your-picovoice-access-key-here

# =============================================================================
# WAKE WORD DETECTION
# =============================================================================

# Wake word to use (default: porcupine)
# Available wake words depend on your Picovoice model
WAKE_WORD=porcupine

# =============================================================================
# AUDIO CONFIGURATION
# =============================================================================

# Audio devices (PipeWire echo cancellation nodes)
# These are automatically configured by the wrapper during installation
MIC_DEVICE=echo_cancel.mic
SPEAKER_DEVICE=echo_cancel.speaker

# Sample rate in Hz
# Common values: 16000 (16kHz - most compatible), 44100 (CD quality), 48000 (professional)
# Adjust based on your audio hardware capabilities
# If you get "Invalid sample rate" errors, try different values
SAMPLE_RATE=16000

# =============================================================================
# OPENTELEMETRY (Optional but Recommended)
# =============================================================================

# Enable/disable telemetry collection
OTEL_ENABLED=true

# Local OTEL collector endpoint (running on the same device)
OTEL_EXPORTER_ENDPOINT=http://localhost:4318

# =============================================================================
# NOTES
# =============================================================================
#
# 1. The wrapper's install.sh will automatically populate most values:
#    - DEVICE_ID (from wrapper .env)
#    - ENV (from wrapper .env)
#    - MIC_DEVICE (from echo cancellation setup)
#    - SPEAKER_DEVICE (from echo cancellation setup)
#    - SAMPLE_RATE (from wrapper .env, defaults to 16000)
#    - OTEL_ENABLED (set to true)
#    - OTEL_EXPORTER_ENDPOINT (set to http://localhost:4318)
#
# 2. You need to manually provide:
#    - SUPABASE_URL and SUPABASE_ANON_KEY
#    - EMAIL and PASSWORD
#    - CONVERSATION_ORCHESTRATOR_URL
#    - ELEVENLABS_API_KEY
#    - PICOVOICE_ACCESS_KEY
#
# 3. Optional customizations:
#    - WAKE_WORD (defaults to "porcupine")
#    - SAMPLE_RATE (only if having audio issues)
#
# 4. For automated installation:
#    - Fill all values in the wrapper's .env file
#    - The wrapper will automatically create and populate this file
#
# =============================================================================
