# This is a locked requirements file to avoid dependency conflicts
# Use this if requirements.txt fails with dependency resolution errors
# Install with: pip install -r requirements-lock.txt

# Wake word detection - install runtime first to avoid conflicts
# Note: tflite-runtime may not be available for Python 3.13+, will fall back to onnxruntime
onnxruntime>=1.16.0

# Core dependencies
numpy>=1.24.0
scipy>=1.11.0

# Wake word detection
openwakeword==0.5.1

# Audio processing
pyaudio>=0.2.11
sounddevice>=0.4.6

# WebSocket communication
websockets>=12.0
certifi>=2023.0.0

# Environment and configuration
python-dotenv>=1.0.0

# Device authentication
cryptography>=41.0.0
requests>=2.31.0

# Async HTTP client for WiFi-based geolocation
aiohttp>=3.9.0

# LED control for ReSpeaker (optional)
pixel-ring>=0.1.0

# OpenTelemetry (upgraded to 1.28.x for protobuf 5.x compatibility)
opentelemetry-api==1.28.2
opentelemetry-sdk==1.28.2
opentelemetry-exporter-otlp-proto-http==1.28.2

# ElevenLabs Scribe v2 for wake word verification
elevenlabs>=1.0.0
