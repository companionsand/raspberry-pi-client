# =============================================================================
# REQUIRED - Device Credentials (from admin portal)
# =============================================================================
DEVICE_ID=<your-device-uuid>
DEVICE_PRIVATE_KEY=<base64-encoded-ed25519-private-key>

# =============================================================================
# OPTIONAL - General Configuration
# =============================================================================
# Skip WiFi setup mode on boot (default: false)
# When WiFi setup is enabled, device creates "Kin_Setup" network (password: kinsetup123)
# Connect to it and visit http://192.168.4.1:8080 to configure your home WiFi
# SKIP_WIFI_SETUP=false

# Deployment environment (default: production)
# ENV=production

# =============================================================================
# OPTIONAL - Telemetry Settings
# =============================================================================
# Enable OpenTelemetry (default: true)
# OTEL_ENABLED=true

# OTEL collector endpoint (default: http://localhost:4318)
# Note: Always uses local collector; wrapper forwards to central endpoint
# OTEL_EXPORTER_ENDPOINT=http://localhost:4318

# =============================================================================
# OPTIONAL - WebRTC AEC Configuration
# =============================================================================
# Enable WebRTC software AEC on top of hardware (default: false)
# Set to true to enable software echo cancellation
# Useful for improving echo cancellation beyond hardware capabilities
# USE_WEBRTC_AEC=false

# USB audio delay in milliseconds (default: 100)
# Typical range: 50-200ms depending on USB audio device
# WEBRTC_AEC_STREAM_DELAY_MS=100

# Noise suppression level 0-3 (default: 1)
# 0 = off, 1 = moderate, 3 = maximum
# WEBRTC_AEC_NS_LEVEL=1

# AGC mode (default: 2)
# 1 = adaptive, 2 = fixed digital
# WEBRTC_AEC_AGC_MODE=2

# =============================================================================
# NOTE: Runtime Configuration
# =============================================================================
# Most settings are fetched from backend after authentication:
# - API keys (ElevenLabs, Picovoice, Google)
# - Wake word and ASR similarity threshold
# - LED settings (enabled, brightness)
# - Speaker volume (0-100%)
# - Presence detection threshold and YAMNet weights
# - ReSpeaker hardware tuning parameters
# - Default reactive agent (for fast wake word response)
# - Logging settings (debug log toggles)
#
# Configuration is cached to ~/.kin_config.json for offline boot capability.
